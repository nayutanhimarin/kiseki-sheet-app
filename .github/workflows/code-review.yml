name: Gemini Action Resolve Test

on:
  push:
    branches: [ action-test-2 ] # このブランチにpushされたら実行

jobs:
  resolve-test:
    runs-on: ubuntu-latest
    steps:
      # Geminiのアクションをただ呼び出すだけのステップ
      - name: Attempt to resolve the Gemini Action
        uses: google/gemini-code-review-action@v1